"use strict";

// Activations remain null until the fresh Open Claim deployments are reviewed.
const FRONTEND_ACTIVATIONS = Object.freeze({
  production: null,
  staging: null,
});

module.exports = { FRONTEND_ACTIVATIONS };
